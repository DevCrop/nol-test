# 블루스퀘어 보안 보완 작업기록

## 2026-09-21 진단 원문 기준 재검증 및 누락 보완

- 기존 체크리스트의 일괄 완료 판정을 정정했다. 18개 진단과 계약 정책 9개에 실제 검증 종류 및 미완료를 구분했다.
- 숨김 게시물/API/첨부 우회, 제목/textarea XSS, works GET method override, 감사 로그의 HTTP 200 오판정, 잘못된 개인정보 내보내기 컬럼/로그 인자를 수정했다.
- NPG 이메일/전화/영문 이름 경계값, 실패 횟수 원자 증가, Credential 변경 경쟁, 장기 미접속 최고관리자 예외, XFF 앞부분 위조, 실행 이중 확장자를 보완했다.
- 기존 PDO/보안 클래스를 재사용했다. 새 프레임워크나 Composer 의존성은 추가하지 않았다.
- 실제 multipart 업로드 및 합성 게시물/배너/계정 기반 HTTP·DB QA 49개, 프록시 검사 3개를 추가했다. 원문 CASE.3 경로 17개, 비밀번호 만료 HTTP도 포함한다. 계정 화면 Copy 차단/입력 예외 브라우저 검사도 추가했다.
- 기존 Docker 전체 QA 179 PASS / 0 FAIL, Chrome 16 PASS / 0 FAIL.
- 별도 외부 포트 없는 bluesquare-sourceqa-20260921 프로젝트를 신규 빌드, 새 볼륨에 원본 SQL 전체 자동 적재 후 release check/up/반복 up 성공. 최초 독립 DB 전체 QA 179 PASS / 0 FAIL. 웹·DB 재시작 후 검사를 확대하여 최종 198 PASS / 0 FAIL.
- 원본 SQL/운영 증분 SQL과 놀씨어터 파일은 수정하지 않았다. 실제 이메일 발송이나 운영 TLS 변경은 수행하지 않았다.
- 남은 전수 QA 및 미사용 구형 모듈 여부는 취약점_체크리스트.md에 명시했다. 이전 작업기록의 완료 표현보다 최신 체크리스트 판정을 우선한다.

## 2026-09-21 추가: 놀 관리자 UI 및 MFA 흐름 정렬

- 놀의 aside 계층과 메뉴명을 따라 사이트정보관리 / 계정 및 권한 관리를 분리했다. 계정 그룹은 최고관리자만 표시하며 서버 권한 검사도 유지한다.
- 계정 관리 / 계정 생성 / 작업 이력 / 개인정보 접속기록의 메뉴 정의를 admin/config/account-menu.php 한 곳에서 관리한다.
- 놀의 no-admin-column--detail 폼 구조, 좌우 라벨 배치, 모바일 세로 배치, 권한·상태 라디오, 중앙 버튼을 재사용했다. 별도 카드형 MFA 이메일 설정은 제거했다.
- 이메일은 계정 생성·수정에서만 관리한다. 이전 email.process.php는 인증 후 410 응답으로 폐기하여 숨은 수정 경로를 남기지 않았다.
- MFA는 놀처럼 비밀번호 확인 → 이메일 입력 → 인증번호 받기 → 코드 확인 순서다. 단, 등록 이메일과 일치하는 주소에만 발송하는 검증은 유지한다.
- 기존 공통 SessionIdleTimer, 세션 재발급·서버 유휴 만료·중복 로그인 차단을 유지했다. 놀 파일과 기존 증분 SQL은 변경하지 않았다.
- Docker 전체 자동 QA 146 PASS / 0 FAIL, 실제 Chrome 데스크톱·390px 모바일 QA 14 PASS / 0 FAIL. SMTP 테스트는 외부 발송 없이 개발 outbox로 검증했다.
- 이번 변경은 블루 260918 내부만 수정했다. 실 SMTP·운영 도메인 적용은 여전히 운영값 대기다.

## 2026-09-21 추가: 독립 증분 배포와 인증 공통화

- `sql/release.php check|up`, `sql/release/*.sql`, 구조/체크섬 명세, `.env.production.example`, `운영배포.md` 추가.
- 놀 기준 검증 정책을 블루에 적용하고 세션 타이머 모듈은 양쪽에 같은 코드로 보관했다. 런타임에서 다른 프로젝트를 참조하지 않는다.
- Docker 자동 QA 140 PASS / 0 FAIL. 블루 화면 QA 9 PASS. 검증 범위·운영 대기 사항은 `QA_계정세션_배포_20260921.md` 참고.
- 임시 QA 계정과 임시 DB는 검사 후 삭제했다. 실 운영 DB에는 접속하거나 SQL을 적용하지 않았다.


## 2026-09-21

- 블루스퀘어 `260918`만 수정하고 놀씨어터는 읽기 전용 기준으로 사용했다.
- PHP 7.4 Apache, MySQL 8.0 Docker 구성을 독립 프로젝트명과 포트로 추가했다.
- 소스에 있던 DB·SMTP 비밀값과 하드코딩 관리자 우회 로그인을 제거했다.
- 공통 보안 클래스(환경, 게이트, 신뢰 프록시, 세션, CSRF, 업로드, XSS 정제, 마스킹, 감사/개인정보 로그)를 추가했다.
- 기존 SHA-256 비밀번호의 1회 호환 로그인과 `password_hash()` 자동 전환을 적용했다.
- 이메일 MFA, 로그인 잠금, 세션 재발급/유휴 만료/동시접속, 비밀번호 만료·초기화, 장기 미접속 잠금을 적용했다.
- 관리자 계정별 MFA 이메일 등록 화면을 추가했다.
- SQL Injection, 인증 선검사, IDOR, CSRF, XSS, 업로드·경로 조작, 오류 노출을 보완했다.
- 개인정보 목록 마스킹, 상세 열람 로그, 다운로드 사유·기록, Drag&Copy 제한을 적용했다.
- 관리자 CRUD 감사 로그를 추가했다.
- 원본 `260918.sql`은 수정하지 않고 반복 실행 가능한 `sql/migrate.php`를 추가했다.
- 사용되지 않는 업로더 클래스와 확인된 테스트·백업·디버그 파일을 제거했다. DB 참조가 없는 팝업 테스트 이미지 6개도 삭제했고 테스트 파일명 패턴은 웹에서 계속 차단한다.
- 새 Docker 볼륨에서 덤프를 적재하고 마이그레이션을 2회 실행했다. 두 번째 실행은 변경 없이 통과했다.
- CAPTCHA가 강화된 관리자 세션과 다른 기본 세션을 사용하던 문제를 발견해 네 CAPTCHA 진입점을 공통 세션 설정으로 통합했다.
- 공개·관리자 레거시 예외 응답 32개 파일을 공통 안전 오류 응답으로 바꾸고 실제 예외는 서버 로그에만 남기도록 했다.
- 레거시 공용 업로드 함수도 실제 MIME 검사·서버 난수명·업로드 루트 내부 삭제로 통합하고 SVG 업로드를 제거했다.
- MFA 화면 CSS 경로 오류, 계정보안 화면 레이아웃, 개인정보 상세 이중 인코딩·중첩 폼·다운로드 사유 UI를 브라우저 확인 후 수정했다.
- Headless Chrome에서 로그인, 데스크톱/모바일 관리자, 계정보안 이메일, MFA 6자리/카운트다운, 개인정보 목록 마스킹, 상세 열람·다운로드 사유 UI를 확인하고 `qa/artifacts`에 화면을 저장했다.
- 최종 자동 QA 60개(문법 1, 보안 단위 12, 정적 회귀 14, DB 통합 20, HTTP 13)가 모두 통과했다.
- 최종 상태에서 마이그레이션 반복 실행, 임시 QA 계정·감사/열람 로그 제거, PHP warning/notice/fatal 및 SQL 런타임 오류 부재를 확인했다.
- 놀씨어터 계정·세션 구현과 기능별로 대조해 블루스퀘어에 최고/일반 관리자 역할, 계정 목록·등록·수정·비활성·삭제, 실패/장기 미접속 잠금 해제, 관리자 비밀번호 초기화 후 변경 강제 화면을 추가했다.
- 마지막 최고 관리자 및 본인 계정의 삭제·비활성·권한 강등을 차단하고, 계정 비활성화·비밀번호 초기화 시 기존 로그인 토큰을 즉시 폐기한다.
- 관리자 계정 목록은 이름·이메일을 마스킹하고 상세 열람은 개인정보 접근 기록에 남기며, 계정 CRUD는 감사 로그 화면에서 확인할 수 있게 했다.
- 구형 SHA-256 비밀번호 변경 화면과 처리 경로는 새 계정 보안 화면으로 통합했다.
- 계정 관리 마이그레이션을 반복 실행 검증했고 자동 QA 75개(문법 1, 보안 단위 12, 정적 회귀 17, DB 통합 31, HTTP 14)가 모두 통과했다.
- 실제 Chrome에서 로그인, 세션 카운트다운, 계정 목록·등록 화면·감사 로그를 데스크톱/모바일로 확인했다. 브라우저 계정 등록 E2E도 검증한 뒤 임시 계정과 로그를 제거했다.

## 운영 전환 전 남은 외부값

- 운영 SMTP 계정·앱 비밀번호
- 관리자 계정별 MFA 수신 이메일
- 운영 관리자 도메인과 DNS
- 관리자 허용 IP/CIDR 및 ALB 신뢰 프록시 CIDR
- 운영 CORS Origin
- 기술검토서 TLS ARN의 블루스퀘어 대상 여부
