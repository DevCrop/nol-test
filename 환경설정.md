# 블루스퀘어 환경설정

## 로컬 실행

1. `.env.example`을 기준으로 `.env`를 만든다. 실제 비밀번호는 `.env`에만 둔다.
2. `docker compose down -v --remove-orphans` 후 `docker compose up -d --build web db`를 실행한다.
3. `260918.sql`을 새 DB 볼륨에 적재한다.
4. `docker compose exec -T web php sql/migrate.php up`을 실행한다.
5. `docker compose exec -T web php qa/run-all.php` 결과가 `0 fail`인지 확인한다.

포트는 Web `8628`, MySQL `3328`, phpMyAdmin `9628`이다. 공개 주소는 `http://localhost:8628`, 관리자 주소는 `http://gate.local:8628/admin`이다. hosts 파일 변경은 이 프로젝트가 자동으로 수행하지 않는다. 필요하면 관리자 권한으로 `add-gate-local.bat`을 직접 실행한다.

최초 마이그레이션 시 기존 블루스퀘어 관리자 중 가장 오래된 계정을 `super`로 지정한다. 최고 관리자는 `/admin/pages/account/`에서 관리자 계정과 감사·접근 기록을 관리하며 일반 관리자는 해당 관리 화면에 접근할 수 없다.

## 환경변수

| 그룹 | 키 | 설명 |
|---|---|---|
| 실행 | `APP_ENV` | 로컬 `development`, 운영 `production` |
| 호스트 | `PUBLIC_HOST`, `GATE_HOST`, `GATE_ENFORCE` | 공개/관리자 호스트 분리 |
| 네트워크 | `GATE_ALLOW_IPS`, `TRUSTED_PROXY_CIDRS`, `CORS_ORIGINS` | 쉼표 구분 IP/CIDR/Origin 목록 |
| 세션 | `SESSION_NAME`, `SESSION_LIFETIME`, `SESSION_SECURE`, `SESSION_SAMESITE` | 운영은 Secure 쿠키 사용 |
| MFA | `MFA_ENABLED`, `MFA_TTL_SECONDS`, `MFA_RESEND_SECONDS`, `MFA_MAX_ATTEMPTS`, `SMTP_*` | 인증번호 6자리, 기본 15분/5회/30초 |
| 로그인 | `LOGIN_MAX_ATTEMPTS`, `LOGIN_LOCK_SECONDS` | 기본 5회/15분 |
| 계정 | `PASSWORD_MAX_DAYS`, `ACCOUNT_IDLE_DAYS` | 기본 각각 90일 |
| DB | `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASS` | 기본 DB명 `dbusrbluesq0211` |

## 운영 전환 순서

SMTP 앱 비밀번호, 관리자별 이메일, 운영 게이트 도메인, 허용 IP/CIDR, 신뢰 프록시 CIDR, CORS Origin을 먼저 검증한다. 그 다음 `APP_ENV=production`, `SESSION_SECURE=true`, `GATE_ENFORCE=true`, `MFA_ENABLED=true`를 적용한다. 운영에서 허용 IP 목록이 비어 있으면 관리자 접근은 차단된다.

TLS는 제공된 ARN이 실제 블루스퀘어 인증서인지 확인하기 전에는 변경하지 않는다.
