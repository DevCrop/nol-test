# API 및 요청 처리 규칙

## 인증과 응답

- `/admin`과 하위 경로는 관리자 게이트 호스트에서만 제공한다. 공개 호스트에서는 404를 반환한다.
- 관리자 HTML 요청의 미인증 상태는 로그인으로 이동하고, AJAX 요청은 HTML 대신 JSON 401/403을 반환한다.
- 상태 변경은 POST로 처리하고 CSRF 토큰을 폼 `_csrf` 또는 `X-CSRF-Token` 헤더로 전달한다.
- 운영 오류 응답에 SQL, 절대경로, 스택, DB/SMTP 설정을 포함하지 않는다.

## 입력과 출력

- 사용자 값은 PDO prepared statement로 바인딩한다.
- 컬럼명·정렬방향·페이지 크기처럼 바인딩할 수 없는 값은 서버 허용목록에서 선택한다.
- 정수 ID는 범위를 확인하고, 조회·수정·삭제 시 `sitekey`와 권한을 같은 쿼리/검사에 포함한다.
- 일반 텍스트 출력은 `htmlspecialchars(..., ENT_QUOTES, 'UTF-8')`를 사용한다.
- 제한된 리치 텍스트만 `HtmlSanitizer` 허용 태그를 사용하며 이벤트 속성·스크립트 URL은 제거한다.

## 파일

- 확장자와 `finfo` 실제 MIME을 모두 허용목록과 비교한다.
- 저장 이름은 서버에서 난수로 생성하고 원본 이름을 경로로 사용하지 않는다.
- 삭제와 다운로드는 `realpath` 결과가 지정 업로드 루트 내부인지 확인한다.
- 업로드 경로에서는 PHP 실행을 금지한다. 개인정보 첨부는 직접 URL 접근을 차단한다.

## 개인정보와 로그

- 목록의 이름·이메일·전화번호는 `PiiMask`로 마스킹한다.
- 상세 열람과 다운로드는 관리자·시각·대상·IP를 기록한다. 다운로드에는 5~500자의 사유가 필요하다.
- CRUD 감사 로그에는 성공한 변경만 기록한다. 비밀번호, MFA 번호, 세션 토큰, 개인정보 원문은 기록하지 않는다.
- 개인정보 영역의 복사·잘라내기·드래그·컨텍스트 메뉴를 제한하되 입력 요소와 키보드 접근성은 막지 않는다.

