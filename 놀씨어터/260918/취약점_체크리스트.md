# 놀씨어터 대학로 취약점 체크리스트

## 2026-09-21 추가 검증: 계정·세션 / 운영 증분 SQL

- [코드·자동 QA 확인] 프로젝트별 `sql/release` 보관, 원본 덤프 보존, 사전 검사 및 반복 적용 검증.
- [코드·자동 QA 확인] 쿠키 강화, 서버 기준 세션 타이머, 조회/활동 요청 분리.
- [운영값 대기] 실제 SMTP 송수신·확정 도메인·호스팅 경로 연결·실서버 사본 배포 리허설.
- 전체 자동 QA 295 PASS / 0 FAIL. 상세 범위와 한계: `QA_계정세션_배포_20260921.md`. 적용 절차: `운영배포.md`.


대상: `noltheater-daehakro.com`  
진단: `별첨2. 취약점진단` 14건  
상태: `[ ]` 미완 / `[x]` 완료  
운영 전환: `환경설정.md`  
기록: `작업기록.md`  
호출 규칙: `API_규칙.md`

SQL Injection / CSRF / 비인증 CASE.3 는 블루스퀘어 전용. 여기 넣지 않음.

## 진단 14건

| 완료 | 등급 | 코드 | 항목 | 위치 |
| --- | --- | --- | --- | --- |
| [x] | H | 2-1 (1) | 악성파일 업로드 | `nol-gate/pages/board/ajax/upload.php` |
| [x] | H | 3-5 (1) | 비인증 상태로 중요 페이지 접근 | `AccountController` 로그인 + 최고관리자 |
| [x] | H | 4-2 (1) | 소스코드 내 주요정보 노출 | 비밀은 `.env`만 사용, Compose DB 기본 비밀번호 제거·필수값 검사 |
| [x] | M | 1-7 (1) | XSS | 팝업·FAQ·검색 FAQ·공연 상세 sanitize, 외부 태그 필터 |
| [x] | M | 3-1 (1) | 파라미터 조작 및 어뷰징 | `upload.guard.php` |
| [x] | L | 3-1 CASE.2 | 파라미터 조작 및 어뷰징 | 타 계정 비번 변경은 최고만 |
| [x] | L | 3-5 CASE.2 | 비인증/권한 없이 중요 페이지 접근 | `account/edit.php` |
| [x] | L | 5-1 (1) | 패스워드 정책 미적용 | `AccountValidator` |
| [x] | L | 5-1 CASE.2 | MFA 미적용 | 관리자 로그인 |
| [x] | L | 5-2 (1) | 인증 실패 횟수 제한 없음 | `login.process.php` 5회/15분 |
| [x] | L | 5-5 (1) | 관리자 페이지 노출 | GATE 분리 + `GATE_ALLOW_IPS` IP/CIDR 검증. 운영 IP 값은 배포 시 등록 |
| [x] | L | 6-1 (1) | 시스템 운영정보 노출 | `ClientFault` — 운영 `APP_ENV=production`에서 SQL/경로 메시지 비노출 |
| [x] | LL | 5-4 (1) | 백업 및 테스트 파일 존재 | 공개 차단 + vendor 예제/테스트 404 + 빈 파일·미사용 컴포넌트·팝업 죽은 코드 제거 |
| [x] | LL | 3-2 (1) | 안전하지 않은 세션 사용 | `AuthSession::rotate` |

## 계약 정책 (진단 PDF 밖)

| 완료 | 번호 | 항목 | 구현 |
| --- | --- | --- | --- |
| [x] | 19 | Google 메일 MFA | `nol-gate/lib/mfa.php`, `.env` `MFA_*` `SMTP_*` |
| [x] | 20 | 관리자 서브도메인 분리 + IP/CIDR | GATE 호스트 + `GateAllowlist`; 등록 IP 허용/미등록 IP 차단 QA |
| [x] | 21 | 개인정보 접속기록 / 다운로드 사유 | `nb_admin_privacy_access`, 대관 열람 자동·다운로드 사유 필수 |
| [x] | 23 | 개인정보 마스킹 (NPG 03) | `PiiMask` 대관 목록 성명 + 가이드 기준 이메일 |
| [x] | 26 | 관리자 CRUD 로그 DB 저장 | `nb_admin_audit`, `AuditLogger`, 메뉴 작업 이력 |
| [x] | 27 | 유휴 세션 타임아웃 | `SessionIdleTimeout`, 상단 실시간 카운트다운, 사용자 활동 keepalive, `SESSION_LIFETIME` 동안 무활동 시 자동 로그아웃 |
| [x] | 28 | 패스워드 변경 주기 | `PasswordExpiry`, `PASSWORD_MAX_DAYS` |
| [x] | 29 | 패스워드 초기화 후 변경 강제 | `PasswordChangeGate`, `setting/pwd.php` |
| [x] | 30 | 동일 ID 동시 접속 차단 | `AuthSession::assertExclusive` |
| [x] | 31 | 장기 미접속 계정 자동 잠금 | `AccountIdleLock`, `ACCOUNT_IDLE_DAYS` |
| [x] | 32 | 개인정보 페이지 Drag&Copy 차단 | 대관 목록·상세 `PrivacyCopyLock` |

로그인 필요(401) 시 `AuthSession::denyLogin`이 세션을 비우고 GATE `index.php`로 보낸다. 권한 없음(403)·비번 강제 변경은 로그인으로 안 던진다.

## QA

`qa/*.php` CLI. 컨테이너 `docker exec 260918-web-1 php qa/<파일>.php`  
GATE 분리: `qa/gate_isolation.php`

2026-09-21 전체 결과: **14개 스크립트, 237 pass / 0 fail**, 전체 PHP 문법 검사 통과. 비로그인 `account/access.php` 로그인 알림 1회 확인. 로그인·MFA 공통 입력 컴포넌트, 로딩·유효시간과 관리자 세션 타이머는 데스크톱/모바일 실브라우저 확인 완료.

IP 비우면 사무실 밖에서도 GATE 열림. 운영 전환 때 `GATE_ALLOW_IPS`에 승인 IP/CIDR을 채운다.
