# 놀씨어터 운영 배포 / 증분 SQL

## 보관 파일

- 원본 `260918.sql`은 수정하지 않은 초기 자료다. **실서버에 다시 가져오지 않는다.**
- `sql/release/preflight.sql`: 읽기 전용 사전 검사.
- `sql/release/changes.sql`: 사전 검사 + 필요한 보안 스키마 추가/비밀번호 해시 저장 길이 확장 + 마이그레이션 이력.
- `sql/release/target-schema.json`: 개발 DB의 보안 관련 테이블 구조만 보관.
- `sql/release/manifest.json`: 프로젝트/원본 덤프/배포 파일 SHA-256.
- 실제 관리자 계정·비밀번호·메일 앱 비밀번호·개발 데이터는 증분 SQL에 넣지 않는다.
- 배포본은 한 번 생성한 뒤 덮어쓰지 않는다. 다음 변경은 별도 버전 릴리스로 검토한다.

## 실서버 적용 순서

1. 실서버 DB 전체 백업과 파일 백업을 별도 안전한 위치에 보관하고 복원 가능 여부를 확인한다. MyISAM 테이블이 있으므로 쓰기를 멈춘 유지보수 시간에 일관된 백업을 받는다.
2. 실서버 최신 DB 사본을 격리된 스테이징에 복원한다. 프로젝트별 `.env`를 해당 사본에 연결한다. 아래 검사를 먼저 사본에서 수행한다.
3. 프로젝트 루트에서 `php sql/release.php check` 실행. 실패하면 적용하지 말고 실서버 구조 차이를 검토한다.
4. `php sql/release.php up` 실행. 파일 체크섬·대상 sitekey·필수 컬럼 타입/NULL 조건을 검사하고 첫 SQL 오류에서 중단한다. 같은 도구끼리 동시 실행하지 못하도록 DB 잠금을 사용한다.
5. `check` 재실행, 로그인·비밀번호 변경·권한·게시물·개인정보 기능을 확인한다. 검증된 동일 파일을 유지보수 시간에 실서버에 적용한다.
6. 코드와 운영 설정을 배포하고 세션을 새로 로그인해 검증한다. 성공 후에도 백업을 보존한다.

직접 SQL 실행이 꼭 필요하면 동일 DB 연결에서 `preflight.sql` 후 `changes.sql` 순서로 실행한다. **오류 무시/계속 실행(`--force`) 금지.** 웹 DB 도구의 오류 중단 동작이 불확실하면 CLI 래퍼를 사용한다.

반복 적용 및 중간 적용 후 재실행을 지원한다. 기존 계정 ID/비밀번호/활성 상태를 덮어쓰지 않는다. 다만 기존에 날짜가 없던 계정은 배포 시점으로 로그인·비밀번호 변경 기준일을 초기화한다.


MySQL DDL은 자동 커밋이므로 전체 트랜잭션 롤백이 아니다. 실패하면 로그를 보관하고 원인을 고친 후 재실행하거나 유지보수 중 백업을 복원한다. 운영에서 이전 `sql/migrate.php down`을 사용하지 않는다.
사전 검사는 모든 운영 스키마 차이(인덱스·트리거·기본값 등)를 자동 판정하는 도구가 아니다. 실서버 최신 사본을 통한 사전 검증은 생략할 수 없다.

## 도메인 / 디렉터리 연결

고객이 서브도메인을 확정한 뒤 DNS·인증서를 준비하고, 호스팅에서 연결한다. 실제 도메인과 경로는 운영값 대기다.

놀은 관리자 호스트 DocumentRoot가 `260918/nol-gate`다. Docker `apache.conf`의 공유 `/resource`, `/uploads` Alias와 공개 호스트 관리자 경로 차단 규칙을 함께 적용한다. 관리자 디렉터리만 연결하고 공유 리소스 규칙을 빠뜨리지 않는다.

두 프로젝트의 `apache.conf`는 로컬 gate.local 기준이다. 실제 호스팅 설정에서 확정 도메인으로 수정하며, `.env`의 GATE_HOST 변경만으로 Apache 가상호스트가 변경되지는 않는다.
공개 호스트 관리자 경로 404, 허용 IP 접속 성공, 미허용 IP 403, HTTPS 및 Secure 쿠키를 확인한다.

## 독립 메일 / 세션

- `.env.production.example`은 키와 정책값만 제공한다. 실제 비밀값은 각 프로젝트 `.env` 또는 호스팅 비밀설정에만 넣는다.
- SMTP_USER / SMTP_PASS / SMTP_FROM을 **프로젝트별 다른 발신 계정과 앱 비밀번호**로 설정한다. 다른 프로젝트의 비밀값을 복사하지 않는다.
- 관리자 UI의 수신 이메일은 SMTP 발신자와 다르다. 관리자별 수신 주소를 등록하고 실제 송수신을 검증한다.
- MFA_ENABLED=true 전에 SMTP/수신 이메일을 검증한다. 운영 배포 승인 시 MFA를 끈 상태로 남기지 않는다.
- 쿠키는 HttpOnly, SameSite=Lax, host-only, 운영 Secure이며 프로젝트마다 세션 이름이 다르다.
- 세션 타이머는 두 프로젝트에 같은 SessionIdleTimer 모듈을 사용하되 엔드포인트/응답 필드만 연결한다. 조회는 시간을 연장하지 않고 활동 POST 성공 시 서버 남은 시간을 반영한다. 15초마다 다른 탭 활동·만료 상태를 확인한다.
- 네트워크 단절 시 세션을 연장했다고 표시하지 않는다. 서버 인증이 최종 기준이다.

## Docker 재검증

`docker compose exec web php qa/run-all.php`

`qa/migration-release.php`는 개발 Docker에서만 임시 DB를 생성하여 원본 관리자 테이블 구조 → 부분 적용 → 전체 적용 → 재적용을 검사한다. 실제 계정 데이터를 가져오지 않으며 임시 DB는 검사 후 제거한다.
전체 QA에는 각 프로젝트의 기존 자동 검사도 포함한다. 실 SMTP 송수신, 확정 도메인 TLS, 호스팅 경로 연결은 고객 운영값이 있어야 최종 검증할 수 있다.
